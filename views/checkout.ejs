<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Checkout - Mon E-commerce</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(180deg, #0d0d0d 0%, #1a1a1a 100%);
      color: #fff;
      font-family: 'Poppins', sans-serif;
      padding-top: 80px;
    }

    h2 {
      text-align: center;
      color: #ff8000;
      font-weight: 700;
      margin-bottom: 2rem;
    }

    form {
      max-width: 600px;
      margin: auto;
      background: rgba(255,255,255,0.05);
      padding: 2rem;
      border-radius: 20px;
      box-shadow: 0 10px 25px rgba(255,128,0,0.3);
    }

    label {
      font-weight: 500;
      color: #fff;
    }

    input.form-control, textarea.form-control {
      background: rgba(255,255,255,0.1);
      border: none;
      border-radius: 10px;
      color: #fff;
      padding: 0.75rem;
      transition: all 0.3s ease;
    }

    input.form-control:focus, textarea.form-control:focus {
      background: rgba(255,128,0,0.1);
      outline: none;
      box-shadow: 0 0 10px rgba(255,128,0,0.5);
      color: #fff;
    }

    .cart-list {
      margin-top: 1.5rem;
      margin-bottom: 2rem;
    }

    .list-group-item {
      background: rgba(255,255,255,0.05);
      border: none;
      border-radius: 12px;
      margin-bottom: 0.5rem;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: all 0.3s ease;
    }

    .list-group-item:hover {
      background: rgba(255,128,0,0.1);
      transform: translateY(-2px);
    }

    .badge-total {
      background: #ff8000;
      color: #fff;
      padding: 0.4rem 0.8rem;
      border-radius: 12px;
      font-weight: 600;
    }

    button.btn-success {
      width: 100%;
      background: linear-gradient(90deg, #ff8000, #ff5500);
      border: none;
      padding: 0.75rem;
      font-weight: 600;
      border-radius: 30px;
      transition: all 0.3s ease;
    }

    button.btn-success:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 25px rgba(255,128,0,0.5);
    }

    @media (max-width: 768px) {
      form {
        padding: 1.5rem;
      }
    }
  </style>
</head>
<body>

  <%- include('partials/header') %>

  <h2>Finaliser ma commande 🛒</h2>
  <form action="/cart/checkout" method="POST">
    <div class="mb-3">
      <label for="name">Nom complet *</label>
      <input type="text" class="form-control" id="name" name="name" required>
    </div>

    <div class="mb-3">
      <label for="email">Email *</label>
      <input type="email" class="form-control" id="email" name="email" required>
    </div>

    <div class="mb-3">
      <label for="phone">Téléphone</label>
      <input type="text" class="form-control" id="phone" name="phone">
    </div>

    <div class="mb-3">
      <label for="address">Adresse *</label>
      <textarea class="form-control" id="address" name="address" required></textarea>
    </div>

    <h4>Mon panier :</h4>
    <ul class="list-group cart-list">
      <% let grandTotal = 0; %>
      <% cart.forEach(item => { %>
        <% const total = Number(item.price) * Number(item.quantity); grandTotal += total; %>
        <li class="list-group-item">
          <div><%= item.name %> (x<%= item.quantity %>)</div>
          <span class="badge-total"><%= total.toFixed(2) %> FCFA</span>
        </li>
      <% }) %>
    </ul>

    <div class="mb-3 text-end">
      <strong>Total général : <span class="badge-total"><%= grandTotal.toFixed(2) %> FCFA</span></strong>
    </div>

    <button type="submit" class="btn btn-success">Passer la commande ✅</button>
  </form>

  <%- include('partials/footer') %>
</body>
</html>
