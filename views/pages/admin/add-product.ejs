<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Ajouter un produit - Tableau de bord</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <!-- Bootstrap + Fonts + Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    /* ===== GLOBAL ===== */
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(180deg, #0d0d0d 0%, #1a1a1a 100%);
      color: #fff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }

    /* ===== FORM CARD ===== */
    .form-container {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 20px;
      padding: 2.5rem;
      max-width: 550px;
      width: 100%;
      box-shadow: 0 10px 40px rgba(0, 0, 0, 0.6);
      backdrop-filter: blur(10px);
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .form-container h1 {
      text-align: center;
      margin-bottom: 2rem;
      font-weight: 700;
      color: #ff8000;
    }

    /* ===== INPUTS ===== */
    .form-control, .form-control:focus {
      background: rgba(255,255,255,0.08);
      border: 1px solid rgba(255,255,255,0.2);
      color: #fff;
      border-radius: 12px;
      padding: 0.8rem 1rem;
      transition: all 0.3s ease;
    }

    .form-control:focus {
      box-shadow: 0 0 10px rgba(255,128,0,0.5);
      border-color: #ff8000;
    }

    textarea.form-control {
      resize: none;
      min-height: 100px;
    }

    /* ===== FILE INPUT ===== */
    .form-control[type="file"] {
      background: none;
      border: none;
      padding: 0;
    }

    .custom-file-label {
      display: block;
      padding: 0.8rem 1rem;
      background: rgba(255,255,255,0.08);
      border: 1px solid rgba(255,255,255,0.2);
      border-radius: 12px;
      color: #ccc;
      cursor: pointer;
      text-align: center;
      transition: background 0.3s ease, transform 0.2s ease;
    }

    .custom-file-label:hover {
      background: rgba(255,128,0,0.1);
      transform: scale(1.02);
      color: #ff8000;
    }

    input[type="file"] {
      display: none;
    }

    /* ===== BUTTON ===== */
    .btn-submit {
      width: 100%;
      background: linear-gradient(90deg, #ff8000, #ff5500);
      border: none;
      color: #fff;
      font-weight: 600;
      padding: 0.9rem;
      border-radius: 30px;
      transition: all 0.3s ease;
      margin-top: 1rem;
    }

    .btn-submit:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(255,128,0,0.5);
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 576px) {
      .form-container {
        padding: 2rem 1.5rem;
      }
      .form-container h1 {
        font-size: 1.6rem;
      }
    }
  </style>
</head>
<body>

  <div class="form-container">
    <h1><i class="bi bi-box-seam"></i> Ajouter un produit</h1>

    <form action="/admin/products/add" method="POST" enctype="multipart/form-data">

      <div class="mb-3">
        <input type="text" name="name" class="form-control" placeholder="Nom du produit" required>
      </div>

      <div class="mb-3">
        <textarea name="description" class="form-control" placeholder="Description du produit" required></textarea>
      </div>

      <div class="mb-3">
        <input type="number" step="0.01" name="price" class="form-control" placeholder="Prix (€)" required>
      </div>

      <div class="mb-3">
        <input type="number" name="stock" class="form-control" placeholder="Stock disponible" required>
      </div>

      <div class="mb-3">
        <label for="image" class="custom-file-label"><i class="bi bi-upload"></i> Sélectionner une image</label>
        <input type="file" id="image" name="image" accept="image/*" required>
      </div>

      <button type="submit" class="btn-submit">
        <i class="bi bi-plus-circle"></i> Ajouter le produit
      </button>

    </form>
  </div>

</body>
</html>
