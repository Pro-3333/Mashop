<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Accueil - MaShop</title>
  <meta name="description" content="Découvrez les produits tendance et les nouveautés sur MaShop. Livraison rapide, qualité premium et satisfaction garantie.">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    /* ===== GLOBAL ===== */
    :root {
      --primary: #ff8000;
      --secondary: #ff5500;
      --bg-dark: #0d0d0d;
      --bg-card: rgba(255,255,255,0.05);
      --text-light: #fff;
      --text-muted: #ccc;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: var(--bg-dark);
      color: var(--text-light);
      overflow-x: hidden;
    }

    h1, h2, h3 {
      font-weight: 700;
      letter-spacing: 0.5px;
      margin: 0 0 10px;
    }

    main {
      display: block;
    }

    a {
      text-decoration: none;
      color: inherit;
    }

    .btn-cta {
      display: inline-block;
      background: linear-gradient(90deg, var(--primary), var(--secondary));
      color: #fff;
      padding: 10px 22px;
      border-radius: 25px;
      font-weight: 600;
      transition: all 0.3s ease;
      box-shadow: 0 0 15px rgba(255,128,0,0.4);
      font-size: 0.9rem;
    }

    .btn-cta:hover {
      transform: translateY(-2px);
      box-shadow: 0 0 25px rgba(255,128,0,0.6);
    }

    /* ===== HEADER STICKY ===== */
    header {
      position: sticky;
      top: 0;
      background: rgba(13,13,13,0.95);
      backdrop-filter: blur(10px);
      z-index: 100;
      padding: 10px 20px;
      border-bottom: 1px solid rgba(255,255,255,0.1);
    }

    /* ===== HERO ===== */
    .hero-section {
      margin: 20px;
      border-radius: 16px;
      overflow: hidden;
      position: relative;
    }

    .hero-section img {
      width: 100%;
      height: 260px;
      object-fit: cover;
      border-radius: 16px;
    }

    .hero-overlay {
      position: absolute;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: linear-gradient(180deg, rgba(0,0,0,0.4), rgba(0,0,0,0.8));
      display: flex;
      flex-direction: column;
      justify-content: flex-end;
      padding: 20px;
    }

    .hero-overlay h1 {
      font-size: 1.8rem;
      margin-bottom: 8px;
    }

    /* ===== CATEGORIES ===== */
    .categories {
      padding: 20px;
    }
    .categories h2 {
      font-size: 1.4rem;
      margin-bottom: 15px;
      color: var(--primary);
    }
    .cat-scroll {
      display: flex;
      gap: 20px;
      overflow-x: auto;
      padding-bottom: 10px;
      scrollbar-width: none;
    }
    .cat-scroll::-webkit-scrollbar { display: none; }
    .cat-card {
      flex: 0 0 auto;
      text-align: center;
      font-size: 0.85rem;
      color: var(--text-muted);
      transition: transform 0.3s;
    }
    .cat-card:hover {
      transform: translateY(-5px);
    }
    .cat-card img {
      width: 80px;
      height: 80px;
      object-fit: cover;
      border-radius: 50%;
      border: 2px solid var(--primary);
      background: #111;
      margin-bottom: 8px;
    }

    /* ===== PRODUITS ===== */
    .products-section {
      padding: 20px;
    }
    .section-title {
      font-size: 1.4rem;
      color: var(--primary);
      margin-bottom: 20px;
    }
    .product-card {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: var(--bg-card);
      border-radius: 16px;
      margin-bottom: 15px;
      padding: 12px 16px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      opacity: 0;
      transform: translateY(20px);
    }
    .product-card.visible {
      opacity: 1;
      transform: translateY(0);
    }
    .product-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 4px 20px rgba(255,128,0,0.25);
    }
    .product-card img {
      width: 90px;
      height: 90px;
      object-fit: contain;
      border-radius: 10px;
      background: #111;
      padding: 8px;
    }
    .product-info {
      flex: 1;
      padding: 0 10px;
    }
    .product-info h3 {
      font-size: 1rem;
      margin: 0 0 5px;
      color: #fff;
    }
    .price {
      color: #27ae60;
      font-weight: 600;
      font-size: 0.95rem;
    }

    /* ===== AVANTAGES ===== */
    .advantages-section {
      background: linear-gradient(135deg, #1a1a1a 0%, #0d0d0d 100%);
      padding: 40px 20px;
      text-align: center;
    }
    .advantages-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
      gap: 25px;
      max-width: 1000px;
      margin: 0 auto;
    }
    .advantage-card {
      background: var(--bg-card);
      border-radius: 16px;
      padding: 20px;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      opacity: 0;
      transform: translateY(20px);
    }
    .advantage-card.visible {
      opacity: 1;
      transform: translateY(0);
    }
    .advantage-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 0 20px rgba(255,128,0,0.4);
    }
    .advantage-card h3 {
      color: var(--primary);
      margin-bottom: 0.8rem;
    }

    /* ===== FOOTER ===== */
    footer {
      text-align: center;
      padding: 20px;
      color: var(--text-muted);
      font-size: 0.85rem;
      border-top: 1px solid rgba(255,255,255,0.1);
      margin-top: 40px;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 768px) {
      .product-card {
        flex-direction: column;
        align-items: flex-start;
      }
      .product-card img {
        width: 100%;
        max-height: 160px;
        object-fit: contain;
      }
      .btn-cta {
        align-self: flex-end;
        margin-top: 8px;
      }
      .hero-section img {
        height: 200px;
      }
      .hero-overlay h1 {
        font-size: 1.4rem;
      }
    }
  </style>
</head>

<body>
  
    <%- include('../partials/header') %>
  

  <main>
    <!-- HERO -->
    <section class="hero-section">
      <img src="/image/pexels-ferarcosn-190819.jpg" alt="Promotions en cours" loading="lazy">
      <div class="hero-overlay">
        <h1>Bienvenue sur MaShop</h1>
        <p>Découvrez les meilleures offres du moment ✨</p>
      </div>
    </section>

    <!-- CATEGORIES -->
    <section class="categories">
      <h2>Catégories</h2>
      <div class="cat-scroll">
        <div class="cat-card">
          <img src="/image/pexels-ferarcosn-190819.jpg" alt="Catégorie Mode" loading="lazy">
          <p>Mode</p>
        </div>
        <div class="cat-card">
          <img src="/image/istockphoto-590039970-612x612.jpg" alt="Catégorie Électronique" loading="lazy">
          <p>Électronique</p>
        </div>
        <div class="cat-card">
          <img src="/image/photo-1542291026-7eec264c27ff.jpeg" alt="Catégorie Gaming" loading="lazy">
          <p>Gaming</p>
        </div>
        <div class="cat-card">
          <img src="/img/cat4.png" alt="Catégorie Maison" loading="lazy">
          <p>Maison</p>
        </div>
      </div>
    </section>

    <!-- PRODUITS TENDANCE -->
    <section class="products-section">
      <h2 class="section-title">🔥 Produits Tendance</h2>
      <% if (trendingProducts.length > 0) { %>
        <% trendingProducts.forEach(product => { %>
          <article class="product-card">
            <img src="<%= product.image %>" alt="<%= product.name %>" loading="lazy">
            <div class="product-info">
              <h3><%= product.name %></h3>
              <p class="price"><%= product.price %> €</p>
            </div>
            <a href="/products/<%= product.id %>" class="btn-cta">Voir</a>
          </article>
        <% }) %>
      <% } else { %>
        <p>Aucun produit tendance pour le moment.</p>
      <% } %>
    </section>

    <!-- NOUVEAUTÉS -->
    <section class="products-section">
      <h2 class="section-title">🆕 Nouveautés</h2>
      <% if (newProducts.length > 0) { %>
        <% newProducts.forEach(product => { %>
          <article class="product-card">
            <img src="<%= product.image %>" alt="<%= product.name %>" loading="lazy">
            <div class="product-info">
              <h3><%= product.name %></h3>
              <p class="price"><%= product.price %> €</p>
            </div>
            <a href="/products/<%= product.id %>" class="btn-cta">Voir</a>
          </article>
        <% }) %>
      <% } else { %>
        <p>Aucune nouveauté disponible.</p>
      <% } %>
    </section>

    <!-- AVANTAGES -->
    <section class="advantages-section">
      <div class="advantages-grid">
        <div class="advantage-card">
          <h3>🚚 Livraison rapide</h3>
          <p>Vos commandes arrivent directement chez vous en un temps record.</p>
        </div>
        <div class="advantage-card">
          <h3>⭐ Qualité premium</h3>
          <p>Chaque produit est sélectionné pour sa qualité exceptionnelle.</p>
        </div>
        <div class="advantage-card">
          <h3>💬 Satisfaction garantie</h3>
          <p>Notre service client est là pour vous accompagner à chaque étape.</p>
        </div>
      </div>
    </section>
  </main>

  <footer>
    <%- include('../partials/footer') %>
  </footer>

  <!-- ANIMATIONS -->
  <script>
    const cards = document.querySelectorAll('.product-card, .advantage-card');
    const revealOnScroll = () => {
      cards.forEach(c => {
        const top = c.getBoundingClientRect().top;
        if (top < window.innerHeight - 80) c.classList.add('visible');
      });
    };
    window.addEventListener('scroll', revealOnScroll);
    revealOnScroll();
  </script>
</body>
</html>
