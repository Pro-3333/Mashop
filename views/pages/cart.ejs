<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Panier - Mon E-commerce</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">

  <style>
    /* ===== GLOBAL ===== */
    body {
      background: linear-gradient(180deg, #0d0d0d 0%, #1a1a1a 100%);
      color: #fff;
      font-family: 'Poppins', sans-serif;
      margin: 0;
      padding-top: 80px;
    }

    h1 {
      text-align: center;
      color: #ff8000;
      font-weight: 700;
      margin-bottom: 2rem;
      animation: fadeDown 1s ease;
    }

    @keyframes fadeDown {
      0% { opacity: 0; transform: translateY(-20px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    /* ===== CART CARD ===== */
    .cart-card {
      display: flex;
      align-items: center;
      justify-content: space-between;
      background: rgba(255, 255, 255, 0.08);
      border-radius: 20px;
      padding: 1.2rem;
      margin-bottom: 1rem;
      backdrop-filter: blur(10px);
      transition: transform 0.4s ease, box-shadow 0.4s ease;
      animation: fadeUp 0.8s ease;
    }

    .cart-card:hover {
      transform: translateY(-6px) scale(1.02);
      box-shadow: 0 15px 35px rgba(255,128,0,0.4);
    }

    .cart-card img {
      width: 85px;
      height: 85px;
      object-fit: cover;
      border-radius: 12px;
      margin-right: 1rem;
      transition: transform 0.3s ease, filter 0.3s ease;
    }

    .cart-card img:hover {
      transform: rotate(-2deg) scale(1.1);
      filter: brightness(1.1);
    }

    .product-info {
      flex: 1;
      margin-right: 1rem;
    }

    .product-name {
      font-weight: 600;
      font-size: 1.2rem;
      color: #fff;
    }

    .product-price, .product-total {
      font-weight: 500;
      color: #ff8000;
    }

    .badge-quantity {
      background: linear-gradient(90deg, #ff8000, #ff5500);
      color: #fff;
      font-size: 0.9rem;
      padding: 0.4rem 0.7rem;
      border-radius: 12px;
      display: inline-block;
      margin-bottom: 0.5rem;
      animation: pulse 1.5s infinite;
    }

    @keyframes pulse {
      0% { transform: scale(1); }
      50% { transform: scale(1.12); }
      100% { transform: scale(1); }
    }

    /* ===== BUTTONS ===== */
    .btn-quantity, .btn-remove {
      font-size: 0.85rem;
      padding: 0.3rem 0.6rem;
      border-radius: 6px;
      margin-left: 0.2rem;
      transition: all 0.2s ease;
      cursor: pointer;
    }

    .btn-quantity {
      background: linear-gradient(90deg, #28a745, #20c997);
      border: none;
      color: #fff;
    }

    .btn-quantity:hover {
      background: linear-gradient(90deg, #20c997, #28a745);
      transform: translateY(-2px);
    }

    .btn-remove {
      background: linear-gradient(90deg, #dc3545, #c82333);
      border: none;
      color: #fff;
    }

    .btn-remove:hover {
      background: linear-gradient(90deg, #c82333, #dc3545);
      transform: translateY(-2px);
    }

    .btn-checkout {
      background: linear-gradient(90deg, #ff8000, #ff5500);
      color: #fff;
      border-radius: 30px;
      padding: 0.9rem 2rem;
      font-weight: 600;
      transition: all 0.3s ease;
      text-decoration: none;
      display: inline-block;
    }

    .btn-checkout:hover {
      transform: translateY(-3px);
      box-shadow: 0 6px 25px rgba(255,128,0,0.5);
    }

    /* ===== CART EMPTY ===== */
    .cart-empty {
      text-align: center;
      margin-top: 3rem;
      font-size: 1.2rem;
      color: #ccc;
      animation: fadeIn 1s ease;
    }

    @keyframes fadeIn {
      0% { opacity: 0; }
      100% { opacity: 1; }
    }

    /* ===== CART TOTAL ===== */
    .cart-total {
      display: flex;
      justify-content: space-between;
      align-items: center;
      flex-wrap: wrap;
      margin-top: 2rem;
      padding: 1.2rem;
      background: rgba(255,255,255,0.08);
      border-radius: 20px;
      backdrop-filter: blur(10px);
      animation: fadeUp 1s ease;
    }

    @keyframes fadeUp {
      0% { opacity: 0; transform: translateY(20px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    /* Sticky total on mobile */
    @media (max-width: 768px) {
      .cart-card {
        flex-direction: column;
        align-items: flex-start;
      }
      .product-info {
        margin-bottom: 0.5rem;
      }
      .cart-total {
        position: sticky;
        bottom: 10px;
        width: calc(100% - 2rem);
        flex-direction: column;
        align-items: flex-start;
        gap: 1rem;
        z-index: 100;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <div class="container mt-5 mb-5">
    <h1><i class="bi bi-cart-check"></i> Mon Panier</h1>

    <% if(cart.length === 0){ %>
      <div class="cart-empty">
        <i class="bi bi-emoji-frown fs-2"></i><br>
        Votre panier est vide.
      </div>
    <% } else { %>
      <% let grandTotal = 0; %>
      <% cart.forEach(item => { %>
        <% 
          const price = Number(item.price) || 0; 
          const quantity = Number(item.quantity) || 0;
          const total = price * quantity; 
          grandTotal += total; 
        %>
        <div class="cart-card">
          <img src="/uploads/<%= item.image || 'placeholder.png' %>" alt="<%= item.name %>">
          <div class="product-info">
            <div class="product-name"><%= item.name %></div>
            <div class="product-price"><%= price.toFixed(2) %> FCFA</div>
          </div>
          <div>
            <span class="badge-quantity"><%= quantity %></span>
            <div class="mt-2">
              <form action="/cart/add/<%= item.id %>" method="POST" style="display:inline;">
                <button type="submit" class="btn-quantity"><i class="bi bi-plus-lg"></i></button>
              </form>
              <form action="/cart/remove/<%= item.id %>" method="POST" style="display:inline;">
                <button type="submit" class="btn-remove"><i class="bi bi-dash-lg"></i></button>
              </form>
            </div>
            <div class="product-total mt-2"><%= total.toFixed(2) %> FCFA</div>
          </div>
        </div>
      <% }) %>

      <div class="cart-total">
        <h4>Total général : <span style="color:#ff8000;"><%= grandTotal.toFixed(2) %> FCFA</span></h4>
        <a href="/cart/checkout" class="btn-checkout"><i class="bi bi-credit-card"></i> Passer la commande</a>
      </div>
    <% } %>
  </div>

  <%- include('../partials/footer') %>
</body>
</html>
