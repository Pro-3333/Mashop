<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Résultats pour "<%= query %>" - MaShop</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #0d0d0d 0%, #1a1a1a 100%);
      color: #fff;
      margin: 0;
      padding: 0;
    }

    main.results {
      max-width: 1300px;
      margin: 60px auto;
      padding: 0 20px;
      animation: fadeIn 0.5s ease-in;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    main.results h2 {
      font-size: 1.8rem;
      text-align: center;
      color: #ff8000;
      margin-bottom: 40px;
      letter-spacing: 0.5px;
    }

    .results-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(230px, 1fr));
      gap: 25px;
    }

    .product-card {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 16px;
      padding: 20px;
      text-align: center;
      backdrop-filter: blur(10px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
      transition: all 0.3s ease;
      position: relative;
      overflow: hidden;
    }

    .product-card::before {
      content: "";
      position: absolute;
      top: -100%;
      left: -100%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle at 0 0, rgba(255,128,0,0.1), transparent 50%);
      transition: transform 0.4s;
      transform: scale(0);
    }

    .product-card:hover::before {
      transform: scale(1);
    }

    .product-card:hover {
      transform: translateY(-8px);
      box-shadow: 0 15px 35px rgba(255,128,0,0.25);
    }

    .product-card img {
      max-width: 100%;
      height: 200px;
      object-fit: contain;
      border-radius: 10px;
      margin-bottom: 15px;
      transition: transform 0.3s ease;
    }

    .product-card:hover img {
      transform: scale(1.05);
    }

    .product-card h3 {
      font-size: 1.1rem;
      color: #fff;
      margin-bottom: 8px;
    }

    .product-card .price {
      font-size: 1.05rem;
      font-weight: 600;
      color: #27ae60;
      margin-bottom: 15px;
    }

    .btn-cta {
      display: inline-block;
      padding: 10px 18px;
      border-radius: 8px;
      background: linear-gradient(90deg, #ff8000, #e66d00);
      color: #fff;
      font-size: 0.95rem;
      font-weight: 600;
      text-decoration: none;
      transition: all 0.3s;
    }

    .btn-cta:hover {
      background: linear-gradient(90deg, #ffa040, #ff8000);
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(255,128,0,0.4);
    }

    .no-result {
      text-align: center;
      font-size: 1.2rem;
      color: #bbb;
      margin-top: 40px;
    }

    @media (max-width: 600px) {
      main.results {
        margin: 40px 0;
      }
      main.results h2 {
        font-size: 1.5rem;
        margin-bottom: 30px;
      }
      .product-card {
        padding: 15px;
      }
    }
  </style>
</head>
<body>
  <%- include('./partials/header') %>

  <main class="results">
    <h2>Résultats pour "<%= query %>"</h2>

    <div class="results-grid">
      <% if (products.length > 0) { %>
        <% products.forEach(product => { %>
          <div class="product-card">
            <img src="/uploads/<%= product.image %>" ...>
            <h3><%= product.name %></h3>
            <p class="price"><%= product.price %> FCFA</p>
            <a href="/products/<%= product.id %>" class="btn-cta">Voir le produit</a>
          </div>
        <% }) %>
      <% } else { %>
        <p class="no-result">Aucun produit trouvé pour "<%= query %>"</p>
      <% } %>
    </div>
  </main>

  <%- include('./partials/footer') %>
</body>
</html>
