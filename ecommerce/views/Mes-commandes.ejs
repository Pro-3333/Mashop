<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Mes Commandes - MaShop</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
  <style>
    /* --- GLOBAL --- */
    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #0d0d0d 0%, #1a1a1a 100%);
      color: #fff;
      padding-top: 90px;
      overflow-x: hidden;
    }

    h1 {
      text-align: center;
      font-weight: 700;
      color: #ff8000;
      margin-bottom: 2rem;
      font-size: 2rem;
      letter-spacing: 1px;
    }

    /* --- TABLE --- */
    .desktop-table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0 0.75rem;
    }

    .desktop-table thead th {
      background: rgba(255, 128, 0, 0.2);
      color: #fff;
      font-weight: 600;
      text-align: center;
      padding: 1rem;
      border: none;
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .desktop-table tbody tr {
      background: rgba(255,255,255,0.05);
      border-radius: 15px;
      transition: all 0.3s ease;
    }

    .desktop-table tbody tr:hover {
      transform: translateY(-3px);
      box-shadow: 0 10px 25px rgba(255,128,0,0.4);
    }

    .desktop-table td {
      padding: 1rem;
      text-align: center;
      vertical-align: middle;
    }

    .desktop-table td ul {
      padding-left: 1rem;
      margin: 0;
      list-style: none;
    }

    .desktop-table td ul li {
      margin-bottom: 0.35rem;
      font-size: 0.95rem;
      color: #ddd;
    }

    /* --- STATUS BADGES --- */
    .order-status {
      padding: 0.4rem 0.9rem;
      border-radius: 12px;
      font-weight: 600;
      font-size: 0.9rem;
      color: #000;
      display: inline-block;
      min-width: 100px;
      text-align: center;
      animation: fadeIn 0.4s ease;
    }

    .status-en-attente { background: #ffc107; color: #000; }
    .status-en-cours { background: #17a2b8; color: #fff; }
    .status-expediee { background: #28a745; color: #fff; }
    .status-livree { background: #6f42c1; color: #fff; }

    @keyframes fadeIn {
      from { opacity: 0; transform: scale(0.9); }
      to { opacity: 1; transform: scale(1); }
    }

    /* --- MOBILE CARDS --- */
    .order-card {
      background: rgba(255,255,255,0.05);
      border-radius: 15px;
      padding: 1.25rem;
      margin-bottom: 1.5rem;
      box-shadow: 0 4px 15px rgba(0,0,0,0.3);
      transition: transform 0.3s, box-shadow 0.3s;
      animation: slideUp 0.4s ease;
    }

    @keyframes slideUp {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .order-card:hover {
      transform: translateY(-3px);
      box-shadow: 0 8px 25px rgba(255,128,0,0.3);
    }

    .order-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 0.8rem;
    }

    .order-id {
      font-weight: 600;
      color: #ff8000;
      font-size: 1rem;
    }

    .order-date {
      font-size: 0.9rem;
      color: #aaa;
    }

    .order-products {
      margin-top: 0.6rem;
      padding-left: 1rem;
      color: #ddd;
      font-size: 0.95rem;
    }

    .order-products li {
      margin-bottom: 0.25rem;
    }

    /* --- EMPTY STATE --- */
    .no-orders {
      text-align: center;
      margin-top: 4rem;
      color: #ccc;
      font-size: 1.2rem;
      opacity: 0.8;
      animation: fadeIn 0.5s ease;
    }

    /* --- RESPONSIVE --- */
    @media (max-width: 768px) {
      .desktop-table { display: none; }
    }

    @media (min-width: 769px) {
      .order-card { display: none; }
    }
  </style>
</head>
<body>
  <%- include('partials/header') %>

  <div class="container my-5">
    <h1>📦 Mes Commandes</h1>

    <% if (orders.length === 0) { %>
      <div class="no-orders">
        Vous n’avez passé aucune commande pour le moment.<br>
        <a href="/products" style="color:#ff8000; text-decoration:none; font-weight:600;">Découvrir nos produits →</a>
      </div>
    <% } else { %>

      <!-- TABLE DESKTOP -->
      <div class="table-responsive">
        <table class="desktop-table">
          <thead>
            <tr>
              <th>ID</th>
              <th>Total</th>
              <th>Statut</th>
              <th>Produits</th>
              <th>Date</th>
            </tr>
          </thead>
          <tbody>
            <% orders.forEach(order => { %>
              <tr>
                <td>#<%= order.id %></td>
                <td><strong><%= order.total %> €</strong></td>
                <td>
                  <% 
                    let statusClass = '';
                    switch(order.status) {
                      case "En attente": statusClass = 'status-en-attente'; break;
                      case "En cours": statusClass = 'status-en-cours'; break;
                      case "Expédiée": statusClass = 'status-expediee'; break;
                      case "Livrée": statusClass = 'status-livree'; break;
                    }
                  %>
                  <span class="order-status <%= statusClass %>"><%= order.status %></span>
                </td>
                <td>
                  <ul>
                    <% order.OrderItems.forEach(item => { %>
                      <li><%= item.Product ? item.Product.name : "Produit supprimé" %> (x<%= item.quantity %>) - <%= item.price %> €</li>
                    <% }) %>
                  </ul>
                </td>
                <td><%= order.createdAt.toLocaleDateString("fr-FR") %></td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>

      <!-- MOBILE CARDS -->
      <% orders.forEach(order => { %>
        <div class="order-card">
          <div class="order-header">
            <div class="order-id">Commande #<%= order.id %></div>
            <div class="order-date"><%= order.createdAt.toLocaleDateString("fr-FR") %></div>
          </div>
          <% 
            let statusClass = '';
            switch(order.status) {
              case "En attente": statusClass = 'status-en-attente'; break;
              case "En cours": statusClass = 'status-en-cours'; break;
              case "Expédiée": statusClass = 'status-expediee'; break;
              case "Livrée": statusClass = 'status-livree'; break;
            }
          %>
          <div class="order-status <%= statusClass %>"><%= order.status %></div>
          <ul class="order-products mt-2">
            <% order.OrderItems.forEach(item => { %>
              <li><%= item.Product ? item.Product.name : "Produit supprimé" %> (x<%= item.quantity %>) - <%= item.price %> €</li>
            <% }) %>
          </ul>
          <div class="mt-2"><strong>Total :</strong> <%= order.total %> €</div>
        </div>
      <% }) %>

    <% } %>
  </div>

  <%- include('partials/footer') %>
</body>
</html>
