<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Admin Login</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css"> <!-- pour icônes -->

  <style>
    /* ===== GLOBAL ===== */
    body {
      margin: 0;
      padding-top: 80px; /* espace pour header fixe */
      font-family: 'Poppins', sans-serif;
      background: #0d0d0d;
      color: #fff;
    }

    /* ===== HERO LOGIN ===== */
    .hero-section {
      background: radial-gradient(circle at 30% 30%, rgba(255,128,0,0.15), transparent 60%),
                  radial-gradient(circle at 70% 70%, rgba(255,255,255,0.05), transparent 70%);
      padding: 6rem 2rem 4rem 2rem;
      text-align: center;
      position: relative;
      overflow: hidden;
      animation: bgPulse 15s infinite alternate;
    }

    @keyframes bgPulse {
      0% { background-position: 0% 0%, 100% 100%; }
      50% { background-position: 10% 10%, 90% 90%; }
      100% { background-position: 0% 0%, 100% 100%; }
    }

    .hero-title {
      font-size: 2.8rem;
      font-weight: 700;
      color: #fff;
      margin-bottom: 1rem;
    }

    .hero-title span {
      color: #ff8000;
      position: relative;
    }

    .hero-title span::after {
      content: "";
      position: absolute;
      bottom: -6px;
      left: 0;
      width: 100%;
      height: 3px;
      background: linear-gradient(90deg, #ff8000, #ff5500);
      border-radius: 2px;
    }

    .hero-subtitle {
      color: #ccc;
      font-size: 1.1rem;
      max-width: 700px;
      margin: 0 auto 2rem auto;
    }

    /* ===== FORMULAIRE LOGIN ===== */
    .login-form {
      background: rgba(255,255,255,0.08);
      padding: 2.5rem;
      border-radius: 20px;
      max-width: 400px;
      margin: 0 auto 4rem auto;
      box-shadow: 0 8px 20px rgba(0,0,0,0.5);
      position: relative;
    }

    .login-form .form-control {
      background: #1a1a1a;
      border: 1px solid #333;
      color: #fff;
      padding-left: 2.5rem;
    }

    .login-form .form-control:focus {
      border-color: #ff8000;
      box-shadow: 0 0 8px rgba(255,128,0,0.6);
      background: #1a1a1a;
      color: #fff;
    }

    .login-form label {
      color: #ddd;
      font-weight: 500;
    }

    .login-form .input-icon {
      position: absolute;
      left: 15px;
      top: 38px;
      color: #ff8000;
      font-size: 1rem;
    }

    .login-form button {
      background: linear-gradient(90deg, #ff8000, #ff5500);
      border: none;
      width: 100%;
      padding: 0.8rem;
      font-weight: 600;
      transition: all 0.3s ease;
    }

    .login-form button:hover {
      transform: translateY(-2px);
      opacity: 0.9;
    }

    .login-links {
      text-align: center;
      margin-top: 1rem;
    }

    .login-links a {
      color: #ff8000;
      text-decoration: none;
      margin: 0 0.5rem;
      transition: all 0.3s ease;
    }

    .login-links a:hover {
      text-decoration: underline;
      color: #ff5500;
    }

    /* ===== RESPONSIVE ===== */
    @media (max-width: 992px) {
      .hero-title { font-size: 2.4rem; }
      .hero-subtitle { font-size: 1rem; }
    }

    @media (max-width: 768px) {
      .hero-title { font-size: 2rem; }
      .hero-subtitle { font-size: 0.95rem; }
    }

    @media (max-width: 576px) {
      .hero-title { font-size: 1.6rem; }
      .hero-subtitle { font-size: 0.9rem; }
      .login-form { padding: 2rem; }
    }
  </style>
</head>
<body>
  <!-- HERO LOGIN -->
  <section class="hero-section">
    <h1 class="hero-title">Connexion <span>Admin</span></h1>
    <p class="hero-subtitle">Veuillez entrer vos identifiants pour accéder à l'administration.</p>
  </section>

  <!-- FORMULAIRE LOGIN -->
  <section class="py-5">
    <div class="container login-form position-relative">
      <form action="/admin/login" method="POST">
        <div class="mb-3 position-relative">
          <i class="fa-solid fa-envelope input-icon"></i>
          <label>Email</label>
          <input type="email" class="form-control" name="email" required>
        </div>
        <div class="mb-3 position-relative">
          <i class="fa-solid fa-lock input-icon"></i>
          <label>Mot de passe</label>
          <input type="password" class="form-control" name="password" required>
        </div>
        <button type="submit" class="btn btn-primary">Se connecter</button>
      </form>

      <div class="login-links">
        <a href="/forgot-password">Mot de passe oublié ?</a> | 
        <a href="/">Retour à l'accueil</a>
      </div>
    </div>
  </section>
</body>
</html>
